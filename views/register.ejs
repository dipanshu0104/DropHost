 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/stylesheets/form.css">
</head>
<body>
    <div class="container">
        <img src="images/up.png" alt="logo">
        <h1>Register</h1>
        <% if(again) { %>
          <center><p class="error"><%= again %></p></center>
        <% } %>
  
      <% if(exist) { %>
          <center><p class="error"><%= exist %></p></center>
        <% } %>
       
        <form action="/create" method="post">
            <input type="text" placeholder="Username" name="username" required>
            <input type="password" placeholder="Password" name="password" required> 
            <input type="password" placeholder="Retype" name="retype" required> <br> <br>
            <button class="login-btn" type="submit">Register</button>
        </form>
        <div class="create-account-section">
            <p>Already have an account? <a href="/"> login</a></p>
        </div>
    </div>
    <div class="banner">
        <img src="images/up.png" alt="logo">
       <center> <h1>Drop<span class="right">Host</span></h1> </center>
    </div>
</body>
</html>